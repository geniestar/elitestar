        <div class="favorite-set">
            <div class="listing-sprite listing-delete" data-id="{{id}}" data-role="{{role}}"></div>
            <div class="title hint" data-hint="{{name}}"><a class="hint" data-hint="{{name}}" href="#" data-id="{{id}}" data-role="{{role}}">{{name}}</a></div>
            <div class="clean"></div>
        </div>
